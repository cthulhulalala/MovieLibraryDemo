import React from "react";

import { MovieCardContainer } from "./MovieCardElements";

const MovieCard = () => {
    return <MovieCardContainer>MovieCard</MovieCardContainer>;
};

export default MovieCard;
