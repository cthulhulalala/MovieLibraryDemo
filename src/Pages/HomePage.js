import React from "react";
import MovieCard from "./../Components/MovieCard/index";

const HomePage = () => {
    return (
        <div>
            <MovieCard />
        </div>
    );
};

export default HomePage;
